#!/bin/bash

# exit script if anything returns an error
set -e

SDIR="$(cd "$(dirname "${BASH_SOURCE[0]}")"&&pwd)"

source ${SDIR}/fulcrum-site-lib

setupEnv $@

restoreDatabaseBackup

cloneCodebase

drupalVersion

prepFileSystem

nginxConf

fulcrumDBUser

drushSysVars

drushUserLogin